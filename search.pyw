"""
GitHub Beginner Repo Finder - Windows GUI Version (No Console)

This is a wrapper script that runs the search application without showing a console window.
Double-click this file to run the app on Windows.

For other operating systems, use: python search.py
"""

# Import and run the main application
from search import main

if __name__ == "__main__":
    main()
